# Ignore environment stuff created by direnv
.envrc
.direnv

# OSX
.DS_Store

# DBs
db.sqlite3

# terraform artifacts
terraform/**/*tfplan*
terraform/**/.terraform
terraform/**/*.tfstate*
# but not the lock file
!terraform/**/.terraform.lock.hcl
